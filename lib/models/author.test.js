const pool = require('../utils/pool');
const fs = require('fs');
const Author = require('./author');

describe('Author class', () => {
  beforeEach()

  it('creates an author with insert method', () => {

  });
});
